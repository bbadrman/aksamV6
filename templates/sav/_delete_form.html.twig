<form method="post" action="{{ path('app_sav_delete', {'id': sav.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ sav.id) }}">
    <button class="btn">Delete</button>
</form>
